var listaDeCompras =[];
listaDeCompras [1] = 'Lechuga';
listaDeCompras[3] = 'Tomates'; 

var verLista = listaDeCompras[1];
console.log(verLista);

//Un elemento que podemos incluir es el elemento length, el cual ayuda a saber cuantos elementos hay en una lista.

console.log(listaDeCompras.length); 
//Aqui nos imprime 4 por mas que no haya elementos asignados, hay espacios en uso.

var color = ['Azul', "Verde"];
color.push("Rojo");

console.log(color);